import { assert } from 'chai';
import { PipelineWorker } from '../../../src/worker';

describe('worker', () => {
  it('initializes', () => {
    //TODO(noel): add tests

    assert.isTrue(true);
  });
});
